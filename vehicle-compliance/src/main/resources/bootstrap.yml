spring:
  application:
    name: vehicle-compliance

aws:
  secretsmanager:
    prefix: /config
    defaultContext: vccs
    profileSeparator: .
    failFast: true
    enabled: true

cloud:
  aws:
    region:
      auto: false
      static: eu-west-2
    stack:
      auto: false
